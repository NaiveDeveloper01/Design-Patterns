// Press Shift twice to open the Search Everywhere dialog and type `show whitespaces`,
// then press Enter. You can now see whitespace characters in your code.
public class Main {
    public static void main(String[] args) {
        /*
         * prototype pattern is used when we have standard object that will be general to all
         * but if we want the object's few parameter to be change then we can use prototype
         * pattern
         *
         * Along with prototype pattern we have to use registry pattern where all general objects will be
         * stored
         * */
    }
}